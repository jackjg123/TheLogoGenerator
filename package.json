{
  "name": "thelogogenerator",
  "version": "1.0.0",
  "description": "This is a command-line application that takes in user input to generate a logo and save it as an .svg file.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Grundey",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.4"
  }
}
